import { Camera } from './components/Camera/Camera';
import { CameraType, CameraRef, CameraProps, TakePhotoOptions } from './components/Camera/types';

export { Camera, CameraType, CameraRef, CameraProps, TakePhotoOptions };
